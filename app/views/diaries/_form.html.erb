<%= form_with(model: diary, local: true) do |form| %>
  <%# if diary.errors.any? %>
    <div id="error_explanation">
      <h2><%#= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
      <%# diary.errors.full_messages.each do |message| %>
        <li><%#= message %></li>
      <%# end %>
      </ul>
    </div>
  <%# end %>

  <div class="field">
    <%= form.label :diary_title %>
    <%= form.text_field :diary_title %>
  </div>

  <div class="field">
    <%= form.label :diary_body %>
    <%= form.text_area :diary_body %>
  </div>

  <div class="field">
    <%#= form.attachment_field :diary_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
